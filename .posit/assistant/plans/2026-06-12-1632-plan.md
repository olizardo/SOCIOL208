# Plan: Correct Equations and Verify Cross-References in prest-hits.qmd

This plan outlines the steps to correct a mathematical error, improve the clarity of an equation, and verify all cross-references in `prest-hits.qmd`.

## 1. Correct the Authority Score Equation

*   **File:** `prest-hits.qmd`
*   **Location:** Line 18
*   **Issue:** The equation for the authority score `x^a_i` is `\sum_i a_{ij} x^h_i`. This is incorrect as it sums over the wrong index and does not represent the sum of hub scores of nodes pointing to `i`.
*   **Proposed Change:**
    *   Change the summation index from `i` to `j`.
    *   Use the transposed adjacency matrix element `a_{ji}` to represent incoming links.
    *   Update the hub score index to `j`.
    *   The corrected equation will be: `$$ x^a_i = \sum_j a_{ji} x^h_j $$`

## 2. Improve Clarity of the Covariance Formula

*   **File:** `prest-hits.qmd`
*   **Location:** Line 355
*   **Issue:** The covariance formula `\frac{\sum_{k=1}^N [(\mathbf{a}_i - \mathbf{\bar{a}}_i)(\mathbf{a}_j - \mathbf{\bar{a}}_j)]_k}{N-1}` is correct but uses a notation that could be clearer.
*   **Proposed Change:** Rewrite the equation using a dot product for a more standard and intuitive representation: `$$ cov(i,j) = \frac{(\mathbf{a}_i - \mathbf{\bar{a}}_i) \cdot (\mathbf{a}_j - \mathbf{\bar{a}}_j)}{N-1} $$`

## 3. Verify All Cross-References

*   **File:** `prest-hits.qmd`
*   **Task:** Check all `@` references and markdown links to ensure they are valid.
*   **Steps:**
    1.  **Identify all `@` references:** Scan the document for all instances of `@[a-zA-Z0-9\-\_]+`.
    2.  **Verify Definitions:** For each reference (e.g., `@fig-sub`, `@tbl-ha`), confirm that a corresponding label (e.g., `{#fig-sub}`, `{#tbl-ha}`) exists in the document.
    3.  **Identify all markdown links:** Scan for links of the form `[text](file.qmd)`.
    4.  **Verify File Existence:** For each markdown link, check if the linked file (e.g., `basic.qmd`) exists in the project.
    5.  **Report any broken links:** Document any missing labels or files.

This plan will ensure the mathematical accuracy and integrity of all cross-references in the document.
