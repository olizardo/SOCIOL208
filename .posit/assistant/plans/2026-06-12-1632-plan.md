# Plan: Improve Text Clarity, Grammatical Accuracy, and Plot Label Bug Fixes in prest-hits.qmd

This plan details findings from an in-depth audit of the prose, mathematical explanations, and R code within `prest-hits.qmd`. 

While the sociological and network concepts are exceptionally well explained, we have identified several small grammatical typos, mathematical index inconsistencies, and a notable plotting label swap bug in the code.

---

## 1. Text Typos & Grammatical Corrections

We will correct several minor grammar/spelling issues:
*   **Line 118:** `"off-diagonals cells"` -> `"off-diagonal cells"`
*   **Line 145:** `"If these nodes where papers"` -> `"If these nodes were papers"`
*   **Line 147:** `"is is called"` -> `"is called"` (duplicate word)
*   **Line 255:** `"Each of these matrices are row stochastic"` -> `"Each of these matrices is row stochastic"` (subject-verb agreement)
*   **Line 290:** `"in the this case"` -> `"in this case"`
*   **Line 351:** `"vectors in questions"` -> `"vectors in question"`
*   **Line 378:** `"post-multyplying"` -> `"post-multiplying"`
*   **Line 499:** `"inline 4"` -> `"in line 4"`

---

## 2. Mathematical Explanation & Index Consistency

Following our previous equation corrections, we need to bring the accompanying prose into perfect alignment:
*   **Line 21:** `"sum over i; the indegree"` -> `"sum over j; the indegree"`. (We updated the equation to sum over the index $j$ to match standard convention, so the text description should match).
*   **Line 218:** `"Each non-zero entry of is thus equal to"` -> `"Each non-zero entry of $\mathbf{P}_h$ is thus equal to"`. (Restores the missing matrix name).
*   **Line 228:** `"Like the $\mathbf{P}_{a}$ matrix, the $\mathbf{P}_{a}$ matrix is row-stochastic"` -> `"Like the $\mathbf{P}_{a}$ matrix, the $\mathbf{P}_{h}$ matrix is row-stochastic"`. (Corrects the duplicate matrix name).
*   **Line 527:** `"nodes with positive scores (hub score larger than their authority scores) appear in blue"` -> `"nodes where the hub score is larger than the authority score appear in blue, and nodes where the authority score is larger than the hub score appear in red."`
    *   *Why:* The difference is defined in code as `c <- salsa.a - salsa.h`. Therefore, if `salsa.h > salsa.a` (hub > authority), `c` is actually *negative* (which maps to blue). The original text calling this "positive scores" is mathematically contradictory.

---

## 3. Fixing the Plot Labeling Swap Bug (R Code)

There is an accuracy bug in the code chunk generating the comparison plots (`#| label: fig-ca`):
*   The first plot (lines 540-569), which uses the CA coordinates `h3, a3`, is labeled:
    `p <- p + labs(x = "PCA Hub Score", y = "PCA Authority Score")`
*   The second plot (lines 570-596), which uses the PCA coordinates `h2, a2`, is labeled:
    `p <- p + labs(x = "CA Hub Score", y = "CA Authority Score")`
*   **Proposed Fix:** Swap these labels so the CA coordinates plot correctly displays the `"CA Hub Score"` and `"CA Authority Score"` labels, and the PCA coordinates plot correctly displays the `"PCA Hub Score"` and `"PCA Authority Score"` labels.

---

## 4. Run-on Sentence Refinement for Better Readability

*   **Line 43:** Convert the long run-on explanation into structured, readable sentences.
    *   *Current:* `"The while loop in lines 5-13 then updates the hub scores (to be the sum of the authority scores of each out-neighbor) in line 7 normalize them in line 8 and update the new authority scores..."`
    *   *Proposed:* `"The while loop in lines 5-13 then updates the hub scores (to be the sum of the authority scores of each out-neighbor) in line 7, normalizes them in line 8, and updates the new authority scores..."`

---

## Next Steps

We will proceed to implement these edits systematically across `prest-hits.qmd`. This will make the file completely mathematically consistent, grammatically polished, and mathematically accurate.
